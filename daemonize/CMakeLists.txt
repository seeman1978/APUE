cmake_minimum_required(VERSION 3.20)
project(daemonize)
include_directories(../include)
set(CMAKE_CXX_STANDARD 17)
aux_source_directory(../lib src)
add_executable(daemonize main.cpp single.cpp ../advio/lockfile.cpp ${src})
target_link_libraries(daemonize pthread)
